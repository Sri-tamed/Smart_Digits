import numpy as np
import matplotlib.pyplot as plt

n = range(-2, 6, 1)
y = []

for i in range(len(n)):
    temp = 1 if n[i] >= 0 else 0
    y.append(temp)

plt.stem(n, y)
plt.xlabel('n')
plt.ylabel('u[n]')
plt.title('Unit Step Signal')
plt.grid(True)
plt.show()
